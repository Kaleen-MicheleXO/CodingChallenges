// Implement a function which convert the given boolean value into its string representation.

// Note: Only valid inputs will be given.

//I will need to write a function that returns a string of "true" or "false"
//my parameter will have to be strictly equal to a boolean of true

let booleanToString = (b) => (b === true ? "true" : "false");
